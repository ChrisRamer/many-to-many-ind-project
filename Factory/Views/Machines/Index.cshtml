@{
	Layout = "_Layout";
}

@using Factory.Models
@model List<Factory.Models.Machine>

<h2 id="title">Machines</h1>

 <hr />

	@if (Model.Count == 0)
	{
	  <p>No machines have been added yet.</p>
	}
	else
	{
		@foreach (Machine machine in Model)
		{
		   <li>@Html.ActionLink($"{machine.Name}", "Details", new { id = machine.MachineId })</li>
		}
	}

 <hr />

 <p>
	@Html.ActionLink("Add a new machine", "Create")
	|
	@Html.ActionLink("Go to homepage", "Index", "Home")
 </p>